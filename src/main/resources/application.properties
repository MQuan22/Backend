#spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/knowledgekombat?allowPublicKeyRetrieval=true&useSSL=false}
spring.datasource.url=${DB_URL:jdbc:mysql://13.250.20.232:3306/knowledgekombat?allowPublicKeyRetrieval=true&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC }
#spring.datasource.username=${DB_USER:root}
spring.datasource.username=${DB_USER:adminKK}
#spring.datasource.password=${DB_PASSWORD:QuanNM_0516}
spring.datasource.password=${DB_PASSWORD:KnowledgeKombat}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update